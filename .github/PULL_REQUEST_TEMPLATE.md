<!--
Thanks for sending a patch. Keep this short — a sentence or two per section is
plenty. Delete anything that does not apply.
-->

## What this changes

<!-- What is different after this PR, and why it is worth doing. -->

## How it was tested

<!--
Which of these did you run, and what did you check by hand?

    npm run lint
    npm run typecheck
    npm test

For a scraping change: which real store URLs did you try it against?
-->

## Checklist

- [ ] `npm run lint`, `npm run typecheck` and `npm test` all pass
- [ ] New behaviour has a test — extraction strategies need a fixture in `tests/fixtures/`
- [ ] `CHANGELOG.md` updated under "Unreleased", if this is user-visible
- [ ] No new runtime dependency (or the PR explains why it earns its place)
- [ ] This does not attempt to bypass any site's access controls

<!--
That last box is not boilerplate: CAPTCHA solving, anti-bot bypass, fingerprint
spoofing and proxy rotation are declined on sight, however good the code is.
See CONTRIBUTING.md.
-->
