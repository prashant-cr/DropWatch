services:
  dropwatch:
    build: .
    # Or, once published: image: ghcr.io/prashant-cr/dropwatch:latest
    ports:
      - '3070:3070'
    volumes:
      - dropwatch-data:/data
    restart: unless-stopped

volumes:
  dropwatch-data:
