/// <reference types="vite/client" />

// Declares the ambient module types Vite handles at build time — notably the
// side-effect `import './styles.css'` in main.tsx, which TypeScript 6 rejects
// without a declaration for it.
